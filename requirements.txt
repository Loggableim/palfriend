openai>=1.0.0
numpy>=1.24.0
sounddevice>=0.4.6
websockets>=11.0.0
TikTokLive>=6.0.0
PyYAML>=6.0.1
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.5
python-socketio>=5.10.0
aiosqlite>=0.19.0
pydantic>=2.0.0
vaderSentiment>=3.3.2
python-Levenshtein>=0.21.0
tiktoken>=0.5.0
fish-audio-sdk>=1.0.0
soundfile>=0.12.0
python-dotenv>=1.0.0

# Optional dependencies for enhanced features (RAG engine)
# Note: chromadb requires onnxruntime which may not be available on all platforms (e.g., Windows ARM)
# If installation fails, you can skip these optional dependencies - the application will still work
# chromadb>=1.3.0
# sentence-transformers>=2.2.0

# Build tools (optional - only needed for creating executables)
# PyInstaller 6.x is compatible with Python 3.11 and 3.12 (NOT 3.13+)
# Uncomment the line below if you want to build the launcher executable
# pyinstaller>=6.0.0,<7.0.0
